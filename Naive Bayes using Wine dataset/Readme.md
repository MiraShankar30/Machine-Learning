## Overview:

Used wine dataset from sklearn.datasets to classify wines into 3 categories. <br>
Trained the model using Gaussian and Multinominal classifier and evaluated which model performs better. 
